import { describe } from 'vitest';

// Lesson 2 — Sign the PUT, no DB write.
//
// Placeholder. project-lesson-test-coder fills these gates: the live R2 PUT/HEAD/GET
// run against a bucket the runner cannot reach, so the gates assert against the
// action's source shape and the pure buildObjectKey/extFor seam (node env, no DOM),
// never live execution. Each gate inlines its own helpers — no shared helpers module.
describe.todo('Lesson 2 — presignedPut signs a 5-min PUT and writes no DB row');
