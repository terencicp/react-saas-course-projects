import { describe } from 'vitest';

// Lesson 3 — Browser PUT, HEAD, then insert.
//
// Placeholder. project-lesson-test-coder fills these gates: the live R2 HEAD runs
// against a bucket the runner cannot reach, so the gates assert against finalizeUpload's
// source shape (HEAD-then-insert, size/type from the HEAD not the client claim, the
// file.uploaded audit) and the upload form's two-step flow (node env, no DOM). Each
// gate inlines its own helpers — no shared helpers module.
describe.todo(
  'Lesson 3 — finalizeUpload HEADs then inserts; the XHR upload form',
);
