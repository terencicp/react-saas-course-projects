'use client';

export const ThemeToggle = () => {
  // TODO(L11) — useTheme()-driven Button, CSS-only Sun/Moon swap, no mount gate
  return (
    <button
      type="button"
      data-testid="theme-toggle"
      aria-label="Toggle theme"
    />
  );
};
