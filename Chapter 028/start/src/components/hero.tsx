export const Hero = () => {
  // TODO(L7) — hero: one <h1>, supporting copy, two CTA buttons, theme-aware image
  return (
    <section data-testid="hero">
      <h1>Themed Product Surface</h1>
    </section>
  );
};
