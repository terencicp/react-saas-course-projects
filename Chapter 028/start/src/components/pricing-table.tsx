export const PricingTable = () => {
  // TODO(L9) — <section> + <h2> + grid mapping pricingTiers, featured lift with motion-reduce:
  return (
    <section id="pricing" data-testid="pricing-table">
      <h2>Pricing</h2>
    </section>
  );
};
