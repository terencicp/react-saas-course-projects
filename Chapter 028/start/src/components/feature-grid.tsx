export const FeatureGrid = () => {
  // TODO(L8) — <section> + <h2> + responsive grid mapping features
  return (
    <section id="features" data-testid="feature-grid">
      <h2>Features</h2>
    </section>
  );
};
