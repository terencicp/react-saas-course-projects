import { describe } from 'vitest';

// Lesson 6 — Rollback (by-hand). The on-disk artifact is the rollback runbook; this
// gate asserts its load-bearing structure (the forward-only-migration caveat, the
// four-step gesture). Filled by project-lesson-test-coder. Node env, no DOM.
describe.todo('Lesson 6 — rollback runbook structure');
