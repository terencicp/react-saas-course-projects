import { describe } from 'vitest';

// Lesson 5 — PR 3 (Contract): drop total, strip legacy references, land the target
// schema. Filled by project-lesson-test-coder. Node env, no DOM.
describe.todo('Lesson 5 — contract: drop total, return the pair directly');
