import { describe } from 'vitest';

// Lesson 4 — PR 2 (Migrate): dual-write all three columns, coalesce dual-read,
// idempotent backfill, promote to NOT NULL. Filled by project-lesson-test-coder.
// Node env, no DOM.
describe.todo('Lesson 4 — migrate: dual-write, dual-read, backfill, promotion');
