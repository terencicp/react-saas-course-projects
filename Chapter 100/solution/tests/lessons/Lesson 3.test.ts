import { describe } from 'vitest';

// Lesson 3 — PR 1 (Expand): add nullable subtotal + tax, generate the additive
// migration. Filled by project-lesson-test-coder. Node env, no DOM.
describe.todo(
  'Lesson 3 — expand: nullable subtotal + tax + additive migration',
);
