const ListDefault = () => {
  // TODO(L2) — same content as @list/page.tsx (the default.tsx that stops a direct /invoices/[id] visit 404ing)
  return (
    <section className="border-border border-e p-4 text-sm text-muted-foreground">
      List slot
    </section>
  );
};

export default ListDefault;
