const DetailDefault = () => {
  // TODO(L2) — "pick an invoice" empty state (data-testid="detail-empty"), NOT a 404
  return (
    <section className="grid place-items-center p-6 text-sm text-muted-foreground">
      Detail slot
    </section>
  );
};

export default DetailDefault;
