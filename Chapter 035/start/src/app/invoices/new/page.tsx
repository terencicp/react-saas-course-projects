const NewPage = () => {
  // TODO(L3) — full-page twin: <InvoiceForm /> + Cancel <Link href="/invoices">
  return (
    <section className="mx-auto flex w-full max-w-lg flex-col gap-6 p-6 text-sm text-muted-foreground">
      New invoice page
    </section>
  );
};

export default NewPage;
