import { describe } from 'vitest';

// Lesson 7 — SUMMARY.md + out-of-scope.md + the two bonus findings. The
// project-lesson-test-coder fills this gate later. It will assert the observable
// shape of findings/SUMMARY.md (a coverage count, the clause-by-clause rubric, the
// per-finding senior-reach detail, the personal checklist, and both bonus findings —
// next/font + the composite index) and findings/out-of-scope.md (at least one
// out-of-category observation) — read as readFileSync source-shape probes (node env,
// no DOM).
describe.todo('Lesson 7 — SUMMARY + out-of-scope + bonus findings 9 & 10');
