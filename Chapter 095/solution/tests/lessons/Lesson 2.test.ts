import { describe } from 'vitest';

// Lesson 2 — Finding 007 (the reference finding): the missing `preload` on the
// marketing hero <Image>. The project-lesson-test-coder fills this gate later. It
// will assert the observable shape of findings/007-missing-priority.md (the four
// sections populated, the 094 L2 rule named, the location naming the file + LCP
// surface, the fix naming `preload` + no-img-element + width/height) — read as
// readFileSync source-shape probes, never importing app code (node env, no DOM).
describe.todo('Lesson 2 — Finding 007 — missing preload on the LCP image');
