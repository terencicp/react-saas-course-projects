# Finding 001 — Sentry is not wired; uncaught errors vanish

<!-- TODO(L3) — document + the Sentry wiring: rule (092 L1), location (the three config files + instrumentation.ts + next.config.ts), surface (/api/test/throw → no event), consequence (3am minified noise), fix (withSentryConfig + source maps + git-SHA release) -->

**Category:**
**Severity:**

## Rule

## Location

## Consequence

## Fix
