# Finding 002 — The webhook logs the `stripe-signature` header in the clear

<!-- TODO(L4) — document the stripe-signature leak in lib/logger.ts: rule (092 L3, 3am rule), location + webhook-replay surface, consequence (secret in logs), fix (one redact seam reused in Pino + beforeSend) -->

**Category:**
**Severity:**

## Rule

## Location

## Consequence

## Fix
