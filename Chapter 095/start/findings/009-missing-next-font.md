# Finding 009 — Marketing page loads its font via a raw `<link>` instead of `next/font`

<!-- TODO(L5 — optional bonus) — document the raw <link> Google font on the marketing surface: rule (094 L1/L2, next/font fallback metrics), location ((marketing)/layout.tsx) + Network surface, consequence (render-blocking font + CLS reflow risk), fix (next/font self-hosted) -->

**Category:**
**Severity:**

## Rule

## Location

## Consequence

## Fix
