// TODO(L5) — read getCurrentUser(), show Hello {name} + session metadata.
const DashboardPage = () => (
  <main data-testid="dashboard-page" className="mx-auto max-w-2xl px-6 py-16">
    <h1 className="text-2xl font-semibold">Dashboard</h1>
  </main>
);

export default DashboardPage;
