// TODO(L6) — money path: storageState admin → /inspector (entitlement-plan: free) → Upgrade (checkout-pro-button) → checkout.stripe.com → fillStripeCard 4242… → /billing/success finalizing → you are all set / your plan is now pro → reload /inspector shows entitlement-plan: pro. Browser state only.
import { test } from './fixtures';

test.fixme('admin can upgrade to Pro via Stripe Checkout', async () => {});
